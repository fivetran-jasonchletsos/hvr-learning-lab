This directory is intentionally empty.

The target database starts with no tables. Module 6 (Initial Load using Refresh) is where HVR creates and populates the `customers` and `orders` tables here for the first time — that's the point of the exercise.
